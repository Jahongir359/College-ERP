# Web framework
Django==3.1.1

# WSGI server (production)
gunicorn==20.0.4

# Static file serving
whitenoise==6.12.0

# PostgreSQL driver (Digital Ocean managed databases)
psycopg2-binary>=2.9.3

# Database URL parsing (reads DATABASE_URL env var)
dj-database-url==0.5.0

# Image handling (profile pictures)
Pillow>=9.0.0

# HTTP requests (Firebase notifications)
requests>=2.27.0

# Environment variable loading from .env
python-dotenv>=0.19.0

# Login brute-force lockout (audit item #15).
# Pinned to the last 5.x line that supports Django 3.1; do NOT upgrade
# without first upgrading Django itself.
django-axes==5.13.1
